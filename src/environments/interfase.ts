export interface InterfaceKey {
  apiKey: string;
  production: boolean;
  fbUrl: string;
  projectId: string;
  firebase: {
    apiKey: string;
    authDomain: string;
    databaseURL: string;
    projectId: string;
    storageBucket: string;
    messagingSenderId: string;
    appId: string;
  };
}

